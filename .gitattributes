# Auto detect text files and perform LF normalization
* text=auto

# Shell scripts are COPYed into a Linux image. Checked out with CRLF on
# Windows, the shebang becomes "#!/bin/sh\r" and the container fails to boot
# with "bad interpreter". Force LF regardless of platform.
*.sh text eol=lf
